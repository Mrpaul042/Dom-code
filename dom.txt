//create element
document.createElement('div');
//innerHTML vs innerText

innerHTML -> suppose, if you are enclosing any html element, innerHTML Will parses the
those html element

innerText-> suppose, if you are enclosing any html element inner Text will render as a normal text

innerHTML or innerText -> choice

using the style we can add css to the html element

how to create HTML element along with class/id attribute ??
use setAttribute method.
2 parameters-> both are strings..
setAttribute('class','classname');
setAttribute('id','classname');

you can add only single class

styling:manually

retrival
already the HTML elements are there in my index.html file , we need to retirve the elements an their values

1.document.getElementById('');retirve single element
null will be the output if id is unavailable

2.document.getElementsByclassname:it returns all the elements whose class name is main
return type is an array[];

3.document.getElementsByTagname:it returns all the elements whose tag name is div
return type is an array[];

4.document.querySelector:document.querySelector('.main');if you have the multiple class elements of same class namespace

it will return the first occuring in html file

it will return the first occurence

it is used for the updation

5.document.querySelectorAll:
it will return all the elements in a collection

1.How to create Tables dynamically along with functions??
we need functions for the repetative tags..
code length is reduced here...

//append vs appendchild
//Add event listener(how to create div dynamically)
//append: can append multiple elements, wheas appendChild can append only 1 elements
return type of append is undefined

return type of the appendchild is a first appended element

Event listener:
we can create the elements dynamically

pagination:
1.create a table using DOM
2.use XML HTTP Request for the json data retrival
3.within Request.onload=function(){
//logic for the printing of data
}
calculator:
all BODMAS operations
using functions

    -> attach a function

HTML-FORMS
1.index.html file create the form elements
2.for js operations js is needed

Prompt: A windows object to read the input from the user end
alert, prompt,dialog..














var container=document.createElement('div');
container.setAttribute('class','container');

var row=document.createElement('div');
row.setAttribute('class','row');


var col=document.createElement('div');
col.setAttribute('class','col');

row.append(col);
container.append(row);
document.body.append(container);


var result=document.getElementById('main1');
console.log(result);
